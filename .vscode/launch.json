{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch program",
            "program": "${workspaceFolder}/src/app.ts",
            "request": "launch",
            "preLaunchTask": "npm: build",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "type": "node"
        }
    ]
}